package protectedex.pack2;

//패키지가 달라도 상속받으면 접근(사용)이 가능하다.
import protectedex.pack1.A;

public class D extends A {
	
	public D() {
		//상속받았기 때문에 사용하지만 매개변수가 없을 때는 생략 가능
		super(); 
		this.field = "yes"; // D가 A를 상속받았기 때문이 사용 가능
		this.method();
	}
}
